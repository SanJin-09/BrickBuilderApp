import SwiftUI

// MARK: - Page
struct SavesPage: View {
    var body: some View {
        VStack {
            VStack{
                Text("Hello, I'm SavesPage")
            }
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity, alignment: .center)
        .background(Color(hex: "#FFFFF8"))
    }
}
